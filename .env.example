# Path to your music library on the host machine.
# Defaults to ./music relative to the compose file.
# MUSIC_DIR=./music

# Soulseek credentials (slskd)
# For the default bundled setup, these can be any non-empty placeholder values.
SLSKD_USERNAME=change-me
SLSKD_PASSWORD=change-me

# Optionally authenticate with an API key.
SLSKD_API_KEY=

# Navidrome credentials
# Set NAVIDROME_USER/NAVIDROME_PASS after completing first-run Navidrome setup
NAVIDROME_USER=admin
NAVIDROME_PASS=

# SoundVault image tag. Defaults to "latest".
# Pin to a version (e.g. 1.2.3) for stability.
SOUNDVAULT_IMAGE_TAG=latest

# Host port for the SoundVault web UI. Defaults to 7070.
# SOUNDVAULT_PORT=8080

# Directory for persistent service data (navidrome DB, slskd state).
# Defaults to ./data relative to the compose file.
# DATA_DIR=./data

# Internal Docker service URLs — only override for non-standard network setups
# NAVIDROME_URL=http://navidrome:4533
# SLSKD_URL=http://slskd:5030

# Optional Last.fm API key for SoundVault Discover.
# This is separate from Navidrome's Last.fm metadata settings in navidrome.toml.
# LASTFM_API_KEY=
